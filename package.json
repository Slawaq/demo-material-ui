{
  "name": "demo-ui",
  "version": "1.0.0",
  "description": "example of materail ui on react js",
  "scripts": {
    "clean": "if exist output rmdir /S /q output",
    "build": "npm run clean && webpack",
    "dev": "npm run build && webpack-dev-server --content-base output/",
    "dev-hot": "npm run build && webpack-dev-server --content-base output/ --hot --inline"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-core": "^5.8.5",
    "babel-loader": "^5.3.2",
    "file-loader": "^0.8.4",
    "material-ui": "^0.10.1",
    "raw-loader": "^0.5.1",
    "react": "^0.13.3",
    "react-tap-event-plugin": "^0.1.7",
    "webpack": "^1.10.5"
  },
  "devDependencies": {
    "react-hot-loader": "^1.2.8",
    "webpack-dev-server": "^1.10.1"
  }
}
